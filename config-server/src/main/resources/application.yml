server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/AashuPal/ekart-config-repo.git
          default-label: main
          search-paths: '{application}'
          clone-on-start: true
          username: AashuPal
          password: ${GIT_TOKEN}
  profiles:
    active:
    - default